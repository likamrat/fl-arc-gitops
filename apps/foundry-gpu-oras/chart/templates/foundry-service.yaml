{{- if .Values.foundry.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-local.foundry.serviceName" . }}
  namespace: {{ include "foundry-local.namespace" . }}
  labels:
    {{- include "foundry-local.labels" . | nindent 4 }}
    app.kubernetes.io/component: foundry
  {{- with .Values.foundry.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.foundry.service.type }}
  {{- if and (eq .Values.foundry.service.type "NodePort") .Values.foundry.service.nodePort }}
  ports:
  - port: {{ .Values.foundry.service.port }}
    targetPort: http
    protocol: TCP
    name: http
    nodePort: {{ .Values.foundry.service.nodePort }}
  {{- else }}
  ports:
  - port: {{ .Values.foundry.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  {{- end }}
  selector:
    {{- include "foundry-local.foundry.selectorLabels" . | nindent 4 }}
{{- end }}